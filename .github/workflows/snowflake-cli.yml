name: Snowflake CLI End-to-End ML Pipeline

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  run-snowflake-cli:
    runs-on: ubuntu-latest
    name: Trigger ML Pipeline, Check Status, Download Model & Upload Artifact

    steps:
      # 1️⃣ Checkout repository
      - name: 🚀 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Install Snowflake CLI (modern CLI: `snow`)
      - name: 🧰 Install Snowflake CLI
        uses: snowflakedb/snowflake-cli-action@v2.0
        with:
          custom-github-ref: "main"

      # 3️⃣ Add Snowflake CLI to PATH
      - name: 🩵 Add Snowflake CLI to PATH
        run: |
          echo "/home/runner/.local/share/uv/tools/snowflake-cli/bin" >> $GITHUB_PATH
          echo "✅ Added Snowflake CLI path to environment."

      # 4️⃣ Create secure Snowflake connection config
      - name: ⚙️ Setup Snowflake Config
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
        run: |
          mkdir -p ~/.snowflake
          cat <<EOF > ~/.snowflake/config.toml
          [connections.default]
          account = "${SNOWFLAKE_ACCOUNT}"
          user = "${SNOWFLAKE_USER}"
          password = "${SNOWFLAKE_PASSWORD}"
          role = "${SNOWFLAKE_ROLE}"
          warehouse = "${SNOWFLAKE_WAREHOUSE}"
          database = "${SNOWFLAKE_DATABASE}"
          schema = "${SNOWFLAKE_SCHEMA}"
          EOF
          chmod 600 ~/.snowflake/config.toml
          echo "✅ Snowflake config.toml created."

      # 5️⃣ Verify CLI installation
      - name: 🔍 Verify Snowflake CLI Installation
        run: |
          echo "🔍 Checking Snowflake CLI installation..."
          which snow || { echo "❌ CLI not found in PATH"; exit 1; }
          snow --version
          echo "✅ Snowflake CLI verified."

      # 6️⃣ Trigger the ML pipeline task
      - name: 🚀 Trigger Snowflake ML Pipeline
        run: |
          echo "🚀 Executing ML pipeline task..."
          snow sql -q "USE WAREHOUSE MLOPS_WH; USE DATABASE POWERCONSUMPTION; USE SCHEMA PUBLIC; EXECUTE TASK TASK_1_DATA_INGESTION;"
          echo "✅ ML pipeline triggered successfully!"

      # 7️⃣ Wait for task completion
      - name: ⏳ Wait for pipeline completion
        run: |
          echo "⏳ Waiting 15 seconds before checking task status..."
          sleep 15

      # 8️⃣ Check task status from Snowflake
      - name: 📊 Check Task Execution Status
        run: |
          echo "📊 Checking task execution status in Snowflake..."
          snow sql -q "USE DATABASE POWERCONSUMPTION; USE SCHEMA PUBLIC; SHOW TASKS LIKE 'TASK_1_DATA_INGESTION';" --format table
          echo "✅ Task status fetched successfully."

      # 9️⃣ Download the latest ONNX model from Snowflake Stage
      - name: 📥 Download latest ONNX model
        run: |
          echo "📜 Listing available models on stage..."
          mkdir -p models
          latest_model=$(snow sql -q "LIST @ML_MODELS_STAGE" --format csv | grep '.onnx' | sort -t',' -k4,4r | head -n 1 | cut -d',' -f1)
          if [ -z "$latest_model" ]; then
            echo "❌ No ONNX files found in @ML_MODELS_STAGE!"
            exit 1
          fi
          echo "✅ Found latest model: $latest_model"
          echo "📥 Downloading model..."
          snow sql -q "GET @$latest_model file://./models/"
          echo "✅ Model downloaded to ./models directory."

      # 🔟 Verify model file presence
      - name: 🔍 Verify Model Download
        run: |
          if ls models/*.onnx 1> /dev/null 2>&1; then
            echo "✅ Model file(s) found in ./models:"
            ls -lh models
          else
            echo "❌ No ONNX model files found in ./models!"
            exit 1
          fi

      # 11️⃣ Upload model as artifact
      - name: 📦 Upload Model Artifact
        uses: actions/upload-artifact@v4
        with:
          name: downloaded-onnx-model
          path: models/
          retention-days: 7

      # ✅ Final Summary
      - name: 📜 Summary
        run: echo "✅ End-to-end Snowflake pipeline executed, task verified, and model uploaded successfully!"
