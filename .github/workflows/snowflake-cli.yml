name: Snowflake Model Registry End-to-End Pipeline

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  run-snowflake-cli:
    runs-on: ubuntu-latest
    name: Trigger ML Pipeline → Verify Task → Export Latest Model → Upload Artifact

    steps:
      # 1️⃣ Checkout repository
      - name: 🚀 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Install Snowflake CLI (`snow`)
      - name: 🧰 Install Snowflake CLI
        uses: snowflakedb/snowflake-cli-action@v2.0
        with:
          custom-github-ref: "main"

      # 3️⃣ Add CLI to PATH
      - name: 🩵 Add Snowflake CLI to PATH
        run: echo "/home/runner/.local/share/uv/tools/snowflake-cli/bin" >> $GITHUB_PATH

      # 4️⃣ Configure Snowflake connection
      - name: ⚙️ Setup Snowflake Config
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
        run: |
          mkdir -p ~/.snowflake
          cat <<EOF > ~/.snowflake/config.toml
          [connections.default]
          account = "${SNOWFLAKE_ACCOUNT}"
          user = "${SNOWFLAKE_USER}"
          password = "${SNOWFLAKE_PASSWORD}"
          role = "${SNOWFLAKE_ROLE}"
          warehouse = "${SNOWFLAKE_WAREHOUSE}"
          database = "${SNOWFLAKE_DATABASE}"
          schema = "${SNOWFLAKE_SCHEMA}"
          EOF
          chmod 600 ~/.snowflake/config.toml
          echo "✅ Snowflake config.toml created."

      # 5️⃣ Verify CLI installation
      - name: 🔍 Verify Snowflake CLI
        run: |
          which snow
          snow --version

      # 6️⃣ Trigger Snowflake ML pipeline task
      - name: 🚀 Trigger Snowflake ML Pipeline
        run: |
          echo "🚀 Executing ML pipeline task..."
          snow sql -q "USE WAREHOUSE MLOPS_WH; USE DATABASE POWERCONSUMPTION; USE SCHEMA PUBLIC; EXECUTE TASK TASK_1_DATA_INGESTION;"
          echo "✅ ML pipeline triggered successfully!"

      # 7️⃣ Wait for task completion
      - name: ⏳ Wait for pipeline completion
        run: |
          echo "⏳ Waiting 15 seconds before checking task status..."
          sleep 15

      # 8️⃣ Check Snowflake task execution status
      - name: 📊 Check Task Execution Status
        run: |
          echo "📊 Checking task status in Snowflake..."
          snow sql -q "USE DATABASE POWERCONSUMPTION; USE SCHEMA PUBLIC; SHOW TASKS LIKE 'TASK_1_DATA_INGESTION';" --format table
          echo "✅ Task status fetched successfully."

      # 9️⃣ Identify latest registered model
      - name: 🔍 Identify Latest Model in Registry
        run: |
          echo "📦 Checking latest registered model..."
          mkdir -p models
          latest_model=$(snow sql -q "SHOW MODELS IN SCHEMA PUBLIC;" --format csv | grep POWER_CONSUMPTION_MODEL | sort -t',' -k3,3r | head -n1 | cut -d',' -f1 | tr -d '"')
          if [ -z "$latest_model" ]; then
            echo "❌ No models found in Model Registry!"
            exit 1
          fi
          echo "✅ Latest registered model: $latest_model"
          echo "LATEST_MODEL=$latest_model" >> $GITHUB_ENV

      # 🔟 Export model from Snowflake Model Registry
      - name: 📤 Export Latest Model from Registry
        run: |
          echo "📤 Exporting $LATEST_MODEL to ./models directory..."
          snow ml model export $LATEST_MODEL --file-format onnx --output-dir ./models
          echo "✅ Model exported successfully."

      # 11️⃣ Verify export success
      - name: 🔍 Verify Exported Model
        run: |
          if ls models/*.onnx 1> /dev/null 2>&1; then
            echo "✅ Model file(s) found:"
            ls -lh models
          else
            echo "❌ No ONNX model files found in ./models!"
            exit 1
          fi

      # 12️⃣ Upload exported model as artifact
      - name: 📦 Upload Exported Model Artifact
        uses: actions/upload-artifact@v4
        with:
          name: exported-onnx-model
          path: models/
          retention-days: 7

      # ✅ Final summary
      - name: 📜 Summary
        run: |
          echo "✅ End-to-end pipeline completed!"
          echo "✅ Task executed, model exported from registry, and artifact uploaded successfully."



