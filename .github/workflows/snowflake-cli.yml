name: Snowflake End-to-End Model Pipeline (JSON Version - Auto Trigger)

on:
  push:
    branches:
      - main   # 🔁 Change this to your desired branch (e.g. "dev" or "*")

jobs:
  run-snowflake-cli:
    runs-on: ubuntu-latest
    name: Trigger Tasks → Wait for Training → Fetch Latest ONNX → Upload

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Install Snowflake CLI and jq
      - name: Install Snowflake CLI
        run: |
          echo "🧰 Installing Snowflake CLI and jq..."
          sudo apt-get update -y && sudo apt-get install -y jq
          pip install --upgrade snowflake-cli-labs
          snow --version
          jq --version

      # 3️⃣ Configure Snowflake credentials
      - name: Configure Snowflake credentials
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
        run: |
          mkdir -p ~/.snowflake
          cat <<EOF > ~/.snowflake/config.toml
          [connections.default]
          account = "${SNOWFLAKE_ACCOUNT}"
          user = "${SNOWFLAKE_USER}"
          password = "${SNOWFLAKE_PASSWORD}"
          role = "${SNOWFLAKE_ROLE}"
          warehouse = "${SNOWFLAKE_WAREHOUSE}"
          database = "${SNOWFLAKE_DATABASE}"
          schema = "${SNOWFLAKE_SCHEMA}"
          EOF
          chmod 600 ~/.snowflake/config.toml
          echo "✅ Snowflake credentials configured."

      # 4️⃣ Trigger the root task so the entire chain (1→2→3→4) runs
      - name: Trigger Snowflake ML pipeline (root task)
        run: |
          echo "🚀 Executing pipeline (root task)..."
          snow sql -q "
            USE WAREHOUSE MLOPS_WH;
            USE DATABASE POWERCONSUMPTION;
            USE SCHEMA PUBLIC;
            EXECUTE TASK TASK_1_DATA_INGESTION;
          "
          echo "✅ Root task executed."

      # 5️⃣ Poll until TASK_3_MODEL_TRAINING succeeded recently
      - name: Wait for TASK_3_MODEL_TRAINING to succeed
        run: |
          echo "⏳ Waiting for TASK_3_MODEL_TRAINING success..."
          ATTEMPTS=20
          SLEEP_SEC=30
          i=1
          while [ $i -le $ATTEMPTS ]; do
            count=$(snow sql -q "
              SELECT COUNT(*) AS C
              FROM TABLE(INFORMATION_SCHEMA.TASK_HISTORY(
                SCHEDULED_TIME_RANGE_START => DATEADD('minute', -45, CURRENT_TIMESTAMP())
              ))
              WHERE name = 'TASK_3_MODEL_TRAINING'
                AND state = 'SUCCEEDED';
            " --format json | jq -r '(try .data catch .) | .[0].C // 0')
            echo "🔎 success_count(last 45m) = ${count}"
            if [ "${count}" != "0" ]; then
              echo "✅ Training task succeeded recently."
              break
            fi
            i=$((i+1))
            echo "…not done yet, sleeping ${SLEEP_SEC}s"
            sleep ${SLEEP_SEC}
          done
          if [ "${count}" = "0" ]; then
            echo "❌ Training task did not report success in time."
            exit 1
          fi

      # 6️⃣ Debug listing
      - name: List ONNX files in @ML_MODELS_STAGE (debug)
        run: |
          echo "📜 Stage listing (JSON):"
          snow sql -q "LIST @ML_MODELS_STAGE PATTERN='.*\\.onnx'" --format json | jq '.data'

      # 7️⃣ Download newest ONNX using JSON parsing
      - name: Download latest ONNX model from stage (JSON)
        run: |
          echo "📥 Fetching latest ONNX model from @ML_MODELS_STAGE..."
          mkdir -p models
          latest_file=$(snow sql -q "LIST @ML_MODELS_STAGE PATTERN='.*\\.onnx'" --format json \
            | jq -r '[.data[] | {name: .name, last_modified: .last_modified}] 
                     | sort_by(.last_modified) 
                     | last 
                     | .name')
          if [ -z "$latest_file" ] || [ "$latest_file" = "null" ]; then
            echo "❌ No ONNX file found in @ML_MODELS_STAGE!"
            exit 1
          fi
          file_rel="${latest_file#ml_models_stage/}"
          echo "✅ Latest ONNX file: $file_rel"
          snow sql -q "GET @ML_MODELS_STAGE/$file_rel file://./models/"
          echo "✅ Model downloaded to ./models"

      # 8️⃣ Verify file(s)
      - name: Verify downloaded model
        run: |
          echo "🔎 Verifying downloaded ONNX..."
          ls -lh models || true
          if ! ls models/*.onnx 1>/dev/null 2>&1; then
            echo "❌ No .onnx files were downloaded."
            exit 1
          fi
          echo "✅ ONNX model present."

      # 9️⃣ Upload artifact
      - name: Upload exported ONNX model
        uses: actions/upload-artifact@v4
        with:
          name: exported-onnx-model
          path: models/
          retention-days: 7

      # 🔟 Summary
      - name: Summary
        run: |
          echo "✅ End-to-end pipeline completed."
          echo "✅ Latest model trained by TASK_3 and fetched (JSON-based) from @ML_MODELS_STAGE."




