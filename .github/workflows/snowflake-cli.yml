name: Snowflake CLI Pipeline Trigger

on:
  workflow_dispatch:  # Manual trigger

jobs:
  run-snowflake-cli:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: 🚀 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Install Snowflake CLI
      - name: 🧰 Install Snowflake CLI
        uses: snowflakedb/snowflake-cli-action@v2.0
        with:
          custom-github-ref: "main"

      # 3️⃣ Add Snowflake CLI to PATH (explicit directory)
      - name: 🩵 Add Snowflake CLI to PATH
        run: |
          echo "/home/runner/.local/share/uv/tools/snowflake-cli/bin" >> $GITHUB_PATH
          echo "✅ Added Snowflake CLI path: /home/runner/.local/share/uv/tools/snowflake-cli/bin"

      # 4️⃣ Debug step — confirm CLI location
      - name: 🔍 Verify Snowflake CLI installation
        run: |
          echo "🔍 Searching for Snowflake CLI binary..."
          find /home/runner/.local -type f -name "snow" | tee snowflake-paths.txt
          echo "🧩 Checking version..."
          snow --version

      # 5️⃣ Configure Snowflake credentials and permissions
      - name: ⚙️ Setup Snowflake Config
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
        run: |
          mkdir -p ~/.snowflake
          cat <<EOF > ~/.snowflake/config.toml
          [connections.default]
          account = "${SNOWFLAKE_ACCOUNT}"
          user = "${SNOWFLAKE_USER}"
          password = "${SNOWFLAKE_PASSWORD}"
          role = "${SNOWFLAKE_ROLE}"
          warehouse = "${SNOWFLAKE_WAREHOUSE}"
          database = "${SNOWFLAKE_DATABASE}"
          schema = "${SNOWFLAKE_SCHEMA}"
          EOF

          # 🔒 Fix permission issue (required by Snowflake CLI)
          chmod 600 ~/.snowflake/config.toml
          echo "✅ Config.toml created with secure permissions."

      # 6️⃣ Test Snowflake CLI connection and version
      - name: 🧩 Test Snowflake CLI
        env:
          SF_SKIP_WARNING_FOR_READ_PERMISSIONS_ON_CONFIG_FILE: true
        run: |
          echo "🧠 Testing Snowflake CLI..."
          which snow
          snow --version
          snow connection test || echo "⚠️ Connection test may need valid credentials."

      # 7️⃣ Run actual Snowflake pipeline or SQL query
      - name: 🚀 Trigger Snowflake ML pipeline
        env:
          SF_SKIP_WARNING_FOR_READ_PERMISSIONS_ON_CONFIG_FILE: true
        run: |
          echo "🚀 Triggering Snowflake ML pipeline..."
          # Replace with your actual Snowflake SQL or stored procedure
          snow sql -q "SELECT current_version();" || (echo "❌ Query failed" && exit 1)

      # 8️⃣ Wrap up
      - name: 📜 Summary
        run: echo "✅ Snowflake CLI pipeline completed successfully!"


