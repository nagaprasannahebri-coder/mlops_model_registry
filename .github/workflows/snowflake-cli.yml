name: Snowflake CLI Pipeline Trigger

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  run-snowflake-cli:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout your repository
      - name: 🚀 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Install the official Snowflake CLI
      - name: 🧰 Install Snowflake CLI
        uses: snowflakedb/snowflake-cli-action@v2.0
        with:
          custom-github-ref: "main"

      # 3️⃣ Add CLI binary paths to the PATH environment variable
      - name: 🩵 Add Snowflake CLI to PATH
        run: |
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          echo "/opt/hostedtoolcache/uv/0.9.1/x86_64" >> $GITHUB_PATH
          echo "✅ PATH updated for Snowflake CLI"

      # 4️⃣ (Optional) Configure Snowflake credentials/config file
      # Place your Snowflake config.toml in the repo root, or skip this if using default config
      - name: ⚙️ Setup Snowflake Config
        run: |
          mkdir -p ~/.snowflake
          if [ -f ./config.toml ]; then
            echo "✅ Found config.toml, copying to ~/.snowflake/"
            cp ./config.toml ~/.snowflake/config.toml
          else
            echo "⚠️ config.toml not found, using default config."
          fi

      # 5️⃣ Verify the installation
      - name: 🧩 Check Snowflake CLI installation
        run: |
          echo "🧩 Checking Snowflake CLI installation..."
          which snow || echo "❌ snow not found in PATH"
          snow --version || echo "❌ Snowflake CLI not responding"

      # 6️⃣ Authenticate (if needed)
      # Use OAUTH / Key pair / Username-password stored as GitHub secrets
      - name: 🔐 Authenticate with Snowflake
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
        run: |
          echo "🔐 Testing connection to Snowflake..."
          snow connection test --account $SNOWFLAKE_ACCOUNT --user $SNOWFLAKE_USER || echo "⚠️ Connection test failed"

      # 7️⃣ Run your Snowflake ML pipeline or SQL script
      - name: 🧠 Trigger Snowflake ML pipeline
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
        run: |
          echo "🚀 Triggering Snowflake ML pipeline..."
          snow sql -q "CALL ml_pipeline_execute();" || echo "⚠️ Pipeline trigger failed"

      # 8️⃣ Wrap up with logs
      - name: 📜 Summarize
        run: |
          echo "✅ Workflow completed!"
